{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "BuildIt Watch Configuration",
  "description": "Schema oficial de configuração para o orquestrador de desenvolvimento contínuo (Watch) do BuildIt.",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "URL do JSON Schema para autocomplete e validação no editor."
    },
    "version": {
      "type": "string",
      "description": "Versão semântica da configuração do projeto."
    },
    "targets": {
      "type": "object",
      "description": "Dicionário de alvos a monitorar continuamente.",
      "additionalProperties": {
        "$ref": "#/definitions/WatchTargetConfig"
      }
    },
    "alvos": {
      "type": "object",
      "description": "Alias em português para targets.",
      "additionalProperties": {
        "$ref": "#/definitions/WatchTargetConfig"
      }
    }
  },
  "definitions": {
    "WatchTargetConfig": {
      "type": "object",
      "required": ["entryPoints"],
      "properties": {
        "default": {
          "type": "boolean",
          "description": "Se true, é monitorado automaticamente quando nenhum alvo específico é passado na CLI.",
          "default": true
        },
        "srcdir": {
          "type": "string",
          "description": "Diretório base dos fontes do alvo observado."
        },
        "distdir": {
          "type": "string",
          "description": "Diretório de destino onde as saídas observadas são salvas."
        },
        "copyFiles": {
          "type": "array",
          "description": "Conjuntos de regras de cópia de arquivos estáticos para o distdir.",
          "items": {
            "type": "object",
            "properties": {
              "basedir": {
                "type": "string",
                "description": "Diretório base de origem dos arquivos."
              },
              "includes": {
                "type": "array",
                "items": { "type": "string" },
                "description": "Lista de padrões glob/brace expansion para incluir."
              },
              "excludes": {
                "type": "array",
                "items": { "type": "string" },
                "description": "Lista de padrões glob/brace expansion para excluir."
              }
            }
          }
        },
        "clean": {
          "oneOf": [
            {
              "type": "object",
              "properties": {
                "includes": {
                  "type": "array",
                  "items": { "type": "string" },
                  "description": "Padrões glob/brace expansion relativos a distdir a remover (use '*' para tudo)."
                },
                "excludes": {
                  "type": "array",
                  "items": { "type": "string" },
                  "description": "Padrões glob/brace expansion relativos a distdir a preservar."
                }
              }
            },
            {
              "type": "array",
              "items": { "type": "string" },
              "description": "Lista de caminhos para limpar antes da primeira compilação."
            }
          ],
          "description": "Regras de limpeza prévia no diretório de saída."
        },
        "entryPoints": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Arquivos de entrada a serem monitorados (relativos a srcdir)."
        },
        "platform": {
          "enum": ["browser", "node", "neutral"],
          "description": "Plataforma alvo de execução.",
          "default": "browser"
        },
        "format": {
          "enum": ["esm", "cjs", "iife"],
          "description": "Formato do módulo gerado.",
          "default": "esm"
        },
        "bundle": {
          "type": "boolean",
          "description": "Se agrupa dependências e imports externos.",
          "default": true
        },
        "minify": {
          "type": "boolean",
          "description": "Se aplica minificação (geralmente false em desenvolvimento).",
          "default": false
        },
        "sourcemap": {
          "oneOf": [
            { "type": "boolean" },
            { "enum": ["linked", "inline", "external"] }
          ],
          "description": "Estratégia de mapa de fontes (recomendado 'inline' em watch para rebuild veloz).",
          "default": "inline"
        },
        "jsx": {
          "enum": ["automatic", "transform", "preserve"],
          "description": "Modo de transformação de JSX/TSX.",
          "default": "automatic"
        },
        "jsxImportSource": {
          "type": "string",
          "description": "Pacote para runtime automático do JSX (ex: 'preact', 'react')."
        },
        "conditions": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Condições personalizadas para resolução de exports."
        },
        "define": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "Mapa de constantes globais em tempo de compilação."
        },
        "drop": {
          "type": "array",
          "items": { "enum": ["console", "debugger"] },
          "description": "Instruções a serem eliminadas do código."
        },
        "external": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Módulos tratados como externos."
        },
        "write": {
          "type": "boolean",
          "description": "Se grava os arquivos gerados no disco a cada alteração.",
          "default": true
        },
        "keepNames": {
          "type": "boolean",
          "description": "Preserva os nomes originais de funções e classes."
        },
        "outfile": {
          "type": "string",
          "description": "Caminho do arquivo de saída gerado a cada alteração."
        },
        "loader": {
          "type": "object",
          "additionalProperties": {
            "enum": [
              "js",
              "jsx",
              "ts",
              "tsx",
              "css",
              "json",
              "text",
              "base64",
              "dataurl",
              "file",
              "binary",
              "empty",
              "copy"
            ]
          },
          "description": "Mapeamento de loaders por extensão."
        },
        "alias": {
          "type": "object",
          "additionalProperties": { "type": "string" },
          "description": "Aliases de importação."
        },
        "inject": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Arquivos para injetar no topo do bundle."
        },
        "banner": {
          "type": "object",
          "properties": {
            "js": { "type": "string" },
            "css": { "type": "string" }
          },
          "description": "Banner adicionado no início dos arquivos gerados."
        },
        "footer": {
          "type": "object",
          "properties": {
            "js": { "type": "string" },
            "css": { "type": "string" }
          },
          "description": "Footer adicionado no final dos arquivos gerados."
        },
        "target": {
          "oneOf": [
            { "type": "string" },
            { "type": "array", "items": { "type": "string" } }
          ],
          "description": "Ambiente alvo ECMAScript."
        },
        "charset": {
          "enum": ["ascii", "utf8"],
          "description": "Conjunto de caracteres."
        },
        "logLevel": {
          "enum": ["verbose", "debug", "info", "warning", "error", "silent"],
          "description": "Nível de detalhamento dos logs de rebuild."
        }
      }
    }
  }
}
