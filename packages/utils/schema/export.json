{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "BuildIt export Configuration",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "URL do JSON Schema do exportador."
    },
    "version": {
      "type": "string",
      "description": "Versão semântica da configuração."
    },
    "modos": {
      "type": "object",
      "description": "Dicionário contendo as configurações de cada modo de exportação.",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "arquivoSaida": {
            "type": "string",
            "description": "Caminho do arquivo Markdown gerado."
          },
          "includes": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Lista de padrões glob para inclusão de arquivos (suporta wildcards e brace expansion como {src,public}/**/*.{ts,tsx})."
          },
          "excludes": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Lista de padrões glob para exclusão de arquivos (ex: **/*.test.ts, **/dist/**)."
          },
          "incluiVersao": {
            "type": "boolean",
            "description": "Se true, injeta a versão atual do projeto no topo."
          },
          "instrucaoCustomizada": {
            "type": "string",
            "description": "Mensagem de orientação para o modelo de IA."
          },
          "default": {
            "type": "boolean",
            "description": "Se o modo deve ser executado por padrão."
          },
          "extensoesPermitidas": {
            "type": "array",
            "items": { "type": "string" },
            "description": "[Legado] Lista de extensões de arquivos a serem capturadas."
          },
          "pastaBase": {
            "type": "string",
            "description": "[Legado] Pasta base onde a busca se inicia."
          },
          "subpastasPermitidas": {
            "type": "array",
            "items": { "type": "string" },
            "description": "[Legado] Lista de subdiretórios que podem ser varridos recursivamente."
          },
          "arquivosRaizPermitidos": {
            "type": "array",
            "items": { "type": "string" },
            "description": "[Legado] Nomes exatos de arquivos localizados diretamente na raiz de pastaBase."
          },
          "caminhosAdicionaisPermitidos": {
            "type": "array",
            "items": { "type": "string" },
            "description": "[Legado] Arquivos ou diretórios localizados fora de pastaBase."
          }
        },
        "required": [
          "arquivoSaida"
        ]
      }
    }
  },
  "required": ["modos"]
}
